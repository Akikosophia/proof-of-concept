<%- include('./partials/head') %>

<!-- <main>

    <article class="audit">

        <a href="/financialReview" class="homeButton">test</a>

        <h1 class="titel"> Prompt</h1>


        <form>
            <ul>
                <% prompts.forEach(prompt => { %>
                    <li>
                        <p class="prompt"><%- prompt.text %></p>
                    </li>
                <% }); %>
            </ul>
        </form>


        <input type="submit" class="submit-input" value="Start the prompt" />
    <button class="cancelButton">cancel</button> -->

    <main>

        <article class="audit">
    
            <!-- <a href="/" class="homeButton">home</a> -->
    
            <h1 class="titel"> Prompt</h1>
                        <form>
                            <ul>
                                <% prompts.forEach(prompt => { %>
                                    <li>
                                        <!-- <h2 class="promptTitel"><%- prompt.label %></h2> -->
                                        <p class="prompt"><%- prompt.text %></p>
                                    </li>
                                <% }); %>
                            </ul>
                        </form>
    
                        <input type="submit" class="submit-input" value="Start the prompt" />
                    <button class="cancelButton">cancel</button>
    
        </article>
    </main>

<%- include('./partials/foot') %>
